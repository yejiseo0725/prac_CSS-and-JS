버튼0 누르면 
- 모든버튼 에 붙은 color 클래스명 제거
- 버튼0 에 color 클래스명 추가
- 모든div 에 붙은 show 클래스명 제거
- div0 에 show 클래스명 추가

// === querySelectorAll('.tab-button')[0]
$('.tab-button').eq(0).on('click', function() {

});